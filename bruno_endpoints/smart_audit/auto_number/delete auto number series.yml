info:
  name: delete auto number series
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url_api_v1}}/auto_number/{{auto_number_series_public_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
