info:
  name: list chart of accounts flat
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url_api_v1}}/chart_of_accounts?view=flat"
  params:
    - name: view
      value: "flat"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
