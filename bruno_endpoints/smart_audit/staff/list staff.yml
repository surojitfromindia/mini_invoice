info:
  name: list staff
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url_api_v1}}/staff?page=1&perPage=20"
  params:
    - name: page
      value: "1"
      type: query
    - name: perPage
      value: "20"
      type: query
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        let response_body = res.getBody();
        let staff_public_id = response_body?.data?.rows?.[0]?.publicId;
        if (staff_public_id) {
          bru.setEnvVar("staff_public_id", staff_public_id)
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
