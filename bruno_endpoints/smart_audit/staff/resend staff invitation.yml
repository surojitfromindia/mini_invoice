info:
  name: resend staff invitation
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url_api_v1}}/staff/invitations/resend"
  body:
    type: json
    data: |-
      {
        "invitationId": "{{staff_invitation_id}}"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        let response_body = res.getBody();
        let invitation_id = response_body?.data?.invitationId;
        let invitation_token = response_body?.data?.invitationToken;
        bru.setEnvVar("staff_invitation_id", invitation_id)
        bru.setEnvVar("staff_invitation_token", invitation_token)

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
