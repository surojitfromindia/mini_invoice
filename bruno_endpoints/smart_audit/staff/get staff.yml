info:
  name: get staff
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url_api_v1}}/staff/{{staff_public_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
