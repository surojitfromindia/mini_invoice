info:
  name: delete staff role
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url_api_v1}}/staff_role/{{staff_role_public_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
