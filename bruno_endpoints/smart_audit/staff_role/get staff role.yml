info:
  name: get staff role
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url_api_v1}}/staff_role/{{staff_role_public_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
