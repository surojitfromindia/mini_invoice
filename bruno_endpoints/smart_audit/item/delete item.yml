info:
  name: delete item
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url_api_v1}}/item/{{item_public_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
