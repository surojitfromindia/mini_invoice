info:
  name: logout
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url_api_v1}}/auth/logout"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
