info:
  name: get customer party
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url_api_v1}}/party/{{customer_party_public_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
