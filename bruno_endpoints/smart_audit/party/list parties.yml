info:
  name: list parties
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url_api_v1}}/party?page=1&perPage=20"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
