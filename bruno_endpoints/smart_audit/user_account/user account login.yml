info:
  name: user account login
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url_api_v1}}/user_account/login"
  body:
    type: json
    data: |-
      {
        "email": "surojit99923@gmail.com",
        "password": "12345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
