info:
  name: create organization
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url_api_v1}}/organization/basic"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
